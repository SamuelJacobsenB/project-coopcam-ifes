import { StyleSheet } from "react-native";

export default StyleSheet.create({
  backContainer: {
    display: "flex",
    alignItems: "center",
    gap: 8,
    marginBottom: 16,
  },
  backText: {
    fontFamily: "Poppins-Regular",
    fontSize: 12,
  },
});
