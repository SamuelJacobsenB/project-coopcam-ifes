import { StyleSheet } from "react-native";

export default StyleSheet.create({
  backContainer: {
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    gap: 8,
    padding: 10,
  },
  backText: {
    fontFamily: "Poppins-Regular",
    fontSize: 12,
  },
});
